# network-security-monitor
Python tool that performs daily network scans using Nmap to detect new devices, dangerous open ports and topology changes on local networks — with automated HTML email alerts.
